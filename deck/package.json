{
  "name": "crux-deck",
  "private": true,
  "scripts": {
    "dev": "slidev slides.md --open",
    "build": "slidev build slides.md --base /crux/deck/",
    "export": "slidev export slides.md --output crux-deck.pdf",
    "serve": "serve dist"
  },
  "dependencies": {
    "@slidev/cli": "^0.50.0",
    "@slidev/theme-default": "latest"
  }
}
